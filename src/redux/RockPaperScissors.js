const initialState = {};

const RockPaperScissors = (state = initialState, action) => {
  switch (
    action.type
    // case "THEM_GIO_HANG":
    //   {
    //   }
    //   break;
    // default:
    //   return { ...state };
  ) {
  }
  return { ...state };
};

export default RockPaperScissors;
